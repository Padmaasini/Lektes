# TalentMesh API - Requirements
# Python 3.11 required (set PYTHON_VERSION=3.11.9 in Render env vars)
# pydantic-core is NOT pinned directly — pydantic pins it automatically.
# Pinning pydantic-core explicitly causes build failures when
# the prebuilt wheel is missing for the host Python version.

# ── Web Framework ──────────────────────────────────────────────
fastapi==0.115.5
uvicorn[standard]==0.32.1
python-multipart==0.0.12

# ── Rate Limiting ──────────────────────────────────────────────
slowapi==0.1.9

# ── Database ───────────────────────────────────────────────────
sqlalchemy==2.0.36

# ── Pydantic — let it resolve pydantic-core itself ────────────
pydantic==2.10.3
pydantic-settings==2.7.0
email-validator==2.2.0

# ── CV Parsing ─────────────────────────────────────────────────
PyMuPDF==1.24.14
python-docx==1.1.2
pdfplumber==0.11.4

# ── LLM / Agentic Pipeline ─────────────────────────────────────
groq==0.13.1
langchain-core==0.3.28
langchain-groq==0.2.3
langgraph==0.2.60

# ── Email ──────────────────────────────────────────────────────
resend==2.5.0

# ── Profile Verification ───────────────────────────────────────
httpx==0.28.1

# ── PDF Report Generation ──────────────────────────────────────
reportlab==4.2.5

# ── Utilities ──────────────────────────────────────────────────
python-dotenv==1.0.1
